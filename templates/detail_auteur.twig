{% extends 'base.twig' %}

{% block main %}
	<h1>Détail d'un auteur</h1>
	<h2>
		{{ auteur.prenom }}
		{{ auteur.nom }}
	</h2>
	<h3>Livres de cet auteur :
	</h3>
	{% if livres|length == 0 %}
		<p>Aucun</p>
	{% else %}
		<div class="grid">
			{% for livre in livres %}
				<a class="card" href="detail_livre.php?isbn={{ livre.isbn }}">
					{{ livre.titre }}
				</a>
			{% endfor %}
		</div>
	{% endif %}
	<p class="backlink">
		<a href="liste_auteurs.php">Retour</a>
	</p>
{% endblock %}
